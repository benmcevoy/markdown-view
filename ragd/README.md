# ragd

Semantic search daemon and cli/

TODO: write this.

```
./ragd


Description:
  CLI tool to index and query semantic embeddings.

Usage:
  ragd [command] [options]

Options:
  -?, -h, --help  Show help and usage information
  --version       Show version information

Commands:
  index <path>   Index the given file or folder.
  new            Create a new sqlite embedding database.
  query <query>  Query the index for semantically matching results.
  start          Start the RAG daemon.
  stop           Stop the RAG daemon.
  status         Get status of daemon.

```